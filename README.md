# password_generator_gui
A small Windows Forms password generator written in C#.
The app provides configurable password length and selectable character sets.
it can optionally copy the generated password to the clipboard automatically.
